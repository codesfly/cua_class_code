# W2 配套代码 · Python 速成①

对应教案《W2 · Python 速成①》。直接下载用，别手敲复制。

## 文件清单

| 文件 | 用途 | 怎么用 |
|---|---|---|
| `w2_demo.py` | 本周收尾脚本：列表 + for + if + 函数驱动鼠标 | 在 W1 沙箱桌面终端跑 `python3 w2_demo.py`（依赖 W1 已装的 pyautogui） |
| `w2_homework.py` | 作业 1 起始框架（补全两处 TODO） | 补全 `click_at` 函数体和 for 循环，跑通让鼠标按列表逐个点 |
| `w2_cheatsheet.md` | 本周语法速查（变量 / 字符串 / 列表 / 字典 / for / if / 函数 各一例） | 随时翻查 |

## 提醒

- 这些脚本要在 **W1 的沙箱桌面**里跑（环境和后面一致；pyautogui 在 W1 已装好）。
- 跑 `w2_demo.py` 会看到鼠标自己在桌面上挨个移动、点击——这是「数据 + 循环 + 函数」第一次驱动真实操作。
